package auth

import (
	"testing"
)

// TestPathMatching is skipped because path patterns have been removed in favor of claims-based authorization
func TestPathMatching(t *testing.T) {
	t.Skip("Path pattern validation has been removed in favor of claims-based authorization")
}

// TestComplexPathScenarios is skipped because path patterns have been removed in favor of claims-based authorization
func TestComplexPathScenarios(t *testing.T) {
	t.Skip("Path pattern validation has been removed in favor of claims-based authorization")
}
